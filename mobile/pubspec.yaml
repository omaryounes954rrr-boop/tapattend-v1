name: tapattend
description: NFC and QR attendance for organizations.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2
  mobile_scanner: ^6.0.2
  nfc_manager: ^3.5.0
  geolocator: ^13.0.2
  uuid: ^4.5.1

flutter:
  uses-material-design: true
